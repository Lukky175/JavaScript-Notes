let b1 = "Munnu"
let g1 = "Munni"

let sentence = `'${g1}' is a "Friend" of '${b1}'`

alert(sentence)

// ` is backtick and inside backtick we can use both single/Double Quotes

// Inserting a String Inside 'Template Literal' is called String Interpolation Like We Did here in this case.